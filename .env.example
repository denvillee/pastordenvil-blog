# The Kit form every signup on the site posts to: "Site signup (denvillee.com)".
# Set the real value in Netlify under Site configuration > Environment variables,
# for production AND deploy previews. The build fails without it rather than
# shipping a form that silently discards addresses.
PUBLIC_KIT_FORM_ID=
